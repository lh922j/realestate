# HTTP 요청 및 재시도
requests==2.31.0
tenacity==8.2.3

# 데이터 처리
pandas==2.1.4
numpy==1.26.3

# 환경변수
python-dotenv==1.0.0

# CLI
click==8.1.7

# 로깅
loguru==0.7.2

# 진행상황 표시
tqdm==4.66.1

# 테스트
pytest==7.4.4
pytest-mock==3.12.0

# Streamlit 대시보드
streamlit>=1.32.0
plotly>=5.18.0
folium>=0.15.0
streamlit-folium>=0.18.0

# 분석 및 시각화 (노트북용)
matplotlib==3.8.2
seaborn==0.13.1
scikit-learn==1.4.0

# 지오코딩 + 공간 분석
# geopandas: conda install geopandas  또는  pip install geopandas
geopandas==0.14.3
shapely==2.0.3

# ML 모델
shap>=0.44.0
lightgbm==4.3.0
xgboost==2.0.3
joblib==1.3.2

# 하이퍼파라미터 최적화
optuna==3.6.1

# AutoML (모델 자동 비교)
# 설치 시 버전 충돌 주의: pycaret은 독립 가상환경 권장
# pip install pycaret
pycaret==3.3.2

# PyTorch는 환경마다 CUDA 버전이 다르므로 별도 설치:
# CPU 전용:
#   pip install torch torchvision
# CUDA 11.8:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# CUDA 12.1:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
